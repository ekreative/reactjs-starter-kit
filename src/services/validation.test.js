/* eslint-env jest */
describe('email validation', () => {
  describe('email is valid', () => {
    it('standart email', () => {})
  })
  describe('email is invalid', () => {
    it('is not email', () => {})
  })
})

describe('password validation', () => {
  describe('password is valid', () => {
    it('standart password', () => {})
  })
  describe('password is invalid', () => {
    it('is not email', () => {})
  })
})
